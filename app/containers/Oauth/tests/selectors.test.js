/* eslint-disable linebreak-style */
// import { fromJS } from 'immutable';
// import { selectOauthDomain } from '../selectors';

describe('selectOauthDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
