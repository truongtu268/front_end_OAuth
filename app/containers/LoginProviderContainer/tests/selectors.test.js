// import { fromJS } from 'immutable';
// import { selectLoginProviderContainerDomain } from '../selectors';

describe('selectLoginProviderContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
